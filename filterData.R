# filter by type (confirmed / deaths / recovered)
## only use scrapeCovidData to scrape the one that you need
## then use tidyCovidData to clean it up

# filer by location (province or location)  (also latitude/longitude!)
## use scrapeCovidData to scrape everything
## then filter by location
## then use tidyCovidData to clean it up

# filter by date
## just use importCovidData, then filter by date

